// Import only the required components and hooks
import WalletProvider, { useWallet } from '../components/wallet/WalletProvider';

// Re-export the provider and hook
export { useWallet };
export default WalletProvider;
